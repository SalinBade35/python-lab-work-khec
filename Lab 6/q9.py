#  Create a script that converts a list of tuples, [(‘a’, 1), (‘b’, 2), (‘c’, 3)], into a dictionary.

t1 = [('a', 1), ('b', 2), ('c', 3)]

dict1 = dict(t1)

# without using the colon we can make newdictionary
    # note bhitra bracket is needed to group the keys and values
    
# new_dict = dict((("Name","Naam"), ("Age","umeer")))